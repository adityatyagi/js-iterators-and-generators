{
  "name": "console-log-my-food",
  "version": "1.0.0",
  "description": "A test app developed while pursuing a course from PluralSight on JavaScript Generators and Iterators.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "launch-db": "json-server --watch ./db.json --port 3001"
  },
  "author": "Aditya Tyagi",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.19.2",
    "json-server": "^0.16.1"
  }
}
